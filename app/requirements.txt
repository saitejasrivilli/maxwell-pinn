# app/requirements.txt
# Used by Streamlit Community Cloud — inference only, no training deps.
# torch CPU build keeps the slug under 1 GB.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1+cpu
numpy>=1.24.0
matplotlib>=3.7.0
streamlit>=1.28.0
